[package]
name = "phase0-spike"
version = "0.0.0"
edition.workspace = true
publish = false

# Throwaway Phase 0 code. See ../../docs/phases/phase-0-spike.md for what it
# proved and ./README.md for what it deliberately does not do.

[lib]
name = "spike"
path = "src/lib.rs"

[dependencies]
anyhow.workspace = true
blake3.workspace = true
fastcdc.workspace = true
memmap2.workspace = true
tokio.workspace = true

hex = "0.4"
rand = "0.8"
quinn = "0.11"
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }
rcgen = "0.13"
rustls-pemfile = "2"
