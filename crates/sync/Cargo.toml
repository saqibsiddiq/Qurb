[package]
name = "qurb-sync"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# Deciding what changed, who changed it, and what to do when two devices
# disagree. Pure logic: no I/O, no clock, no database.

[dependencies]
thiserror = "2"

[dev-dependencies]
proptest = "1"
